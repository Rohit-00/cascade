'use strict';

const AnimatedButton = require('..');
const assert = require('assert').strict;

assert.strictEqual(AnimatedButton(), 'Hello from AnimatedButton');
console.info('AnimatedButton tests passed');
